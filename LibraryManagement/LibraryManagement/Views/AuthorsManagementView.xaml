<Window x:Class="LibraryManagement.Views.AuthorsManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="AuthorsGrid" 
                  ItemsSource="{Binding Authors.Local}"
                  AutoGenerateColumns="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat='dd.MM.yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Click="AddAuthor_Click" Margin="5" Padding="15,5" Background="#4CAF50" Foreground="White"/>
            <Button Content="Удалить" Click="DeleteAuthor_Click" Margin="5" Padding="15,5" Background="#F44336" Foreground="White"/>
            <Button Content="Сохранить" Click="Save_Click" Margin="5" Padding="15,5" Background="#2196F3" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>